diff --git a/build/crawlers/node/fallback.js b/build/crawlers/node/fallback.js
index 9df7d39d4edeef30effb829fe136797efdd216eb..5c2124bd329f55963eb5ea10363bdaf085fb5999 100644
--- a/build/crawlers/node/fallback.js
+++ b/build/crawlers/node/fallback.js
@@ -60,12 +60,21 @@ function createFallbackFilesystem(opts) {
             return null;
         }
         const result = dirNode ?? new Map();
-        for (const entry of dirEntries) {
+        for (let entry of dirEntries) {
             const name = entry.name.toString();
             const childAbsolutePath = absolutePath + path_1.default.sep + name;
             if (ignore(childAbsolutePath)) {
                 continue;
             }
+            // OneDrive placeholders may be reported as symbolic links by readdir.
+            if (process.platform === 'win32' && entry.isSymbolicLink()) {
+                try {
+                    entry = fs_1.default.lstatSync(childAbsolutePath);
+                }
+                catch {
+                    continue;
+                }
+            }
             if (entry.isDirectory()) {
                 // NOTE(@kitten): ".git" and ".hg" check replace the VCS_DIRECTORIES ignore pattern
                 // NOTE(@kitten): `.cxx` is ephemeral and should always be safe to ignore
diff --git a/build/crawlers/node/index.js b/build/crawlers/node/index.js
index 5b3b945f88fd2952dcae19e3e9de34da1c1b3038..b19d17bcab208eb5a7c387f7447ca4ca266e28ba 100644
--- a/build/crawlers/node/index.js
+++ b/build/crawlers/node/index.js
@@ -67,17 +67,29 @@ function find(roots, extensions, ignore, includeSymlinks, rootDir, console, prev
             }
             else {
                 for (let idx = 0; idx < entries.length; idx++) {
-                    const entry = entries[idx];
+                    let entry = entries[idx];
                     const name = entry.name;
+                    const file = directory + path.sep + name;
+                    if (ignore(file)) {
+                        continue;
+                    }
+                    // OneDrive placeholders may be reported as symbolic links by readdir.
+                    if (process.platform === 'win32' && entry.isSymbolicLink()) {
+                        try {
+                            entry = fs.lstatSync(file);
+                        }
+                        catch {
+                            continue;
+                        }
+                    }
                     // NOTE(@kitten): This replaces the VCS_DIRECTORIES ignore pattern
                     // NOTE(@kitten): `.cxx` is ephemeral and should always be safe to ignore
                     const isDirectory = entry.isDirectory();
                     if (isDirectory && (name === '.git' || name === '.hg' || name === '.cxx')) {
                         continue;
                     }
-                    const file = directory + path.sep + name;
                     const isSymbolicLink = entry.isSymbolicLink();
-                    if (ignore(file) || (!includeSymlinks && isSymbolicLink)) {
+                    if (!includeSymlinks && isSymbolicLink) {
                         continue;
                     }
                     // Deriving a normal path above the root dir requires slicing off an up-fragment
